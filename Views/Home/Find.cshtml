@model IEnumerable<User>

    @foreach(var user in Model) {
    <div>
        <a asp-controller="Home" asp-action="CreatePrivateRoom" asp-route-userId="@user.Id" class="room-button">
            @user.UserName
        </a>
    </div>
    }